# 安全策略

## 受支持版本

本项目处于早期阶段，**仅对最新版本（`main` 分支最新提交 / 最新 tag）接受安全修复**。使用旧版本时请先升级。

---

## 报告漏洞

**⚠️ 请勿通过公开 Issue 报告安全漏洞。**

请用以下任一方式私下报告：

- **GitHub Security Advisory**（推荐）：仓库 **Security** 标签页 → **Report a vulnerability**
- 或直接联系仓库维护者（见仓库主页）

**报告中请勿附带真实凭证、token、账号昵称或任何个人数据。** 如需演示，请用占位值。

我们会在 **48 小时内**确认收到，并在 **7 天内**给出初步处置意见（尽力而为，非承诺）。

---

## 威胁模型与已知安全边界

本项目在设计上**面向单用户本机自用**。以下边界是**已知的、刻意的**，但使用者必须理解：

### 1. 凭证处理

- 网关**不存储、不上传**凭证。它从 `-credential` 指定路径（或自动探测的桌面客户端路径）读取凭证，仅用于向上游发起请求。
- 凭证**不写入日志**，也**不写入指标 JSON**。
- 但**凭证以明文形式存放在磁盘上**（由上游桌面客户端写入）。本机上的任何其他进程（包括同用户的任意程序）都能读取它。
- **凭证文件权限在写入路径上不会被主动收紧**：写入使用 `os.WriteFile(..., 0o600)`，而该调用**只在文件新建时应用权限位**。若目标文件已存在且权限较松（如 `0644`），不会自动纠正。

### 2. 网络暴露面

- 默认仅监听 `127.0.0.1`。
- ⚠️ **改为监听 `0.0.0.0` 且未设置 `-api-key` 时，同网段任何人都可以调用你的账号额度。** 这是最容易被忽视的风险。
- **没有任何 Origin / Referer 校验**，也没有 CORS 策略。因此本机浏览器上的任意网页都可能向 `http://127.0.0.1:8787` 发起跨源请求（Go 的 `json.Unmarshal` 不校验 `Content-Type`，`text/plain` 的简单请求无需预检）。这包括读取 `/api/metrics`（含最近 200 条调用记录）与触发 `/v1/*` 消耗额度。
- 网关本身**不是为公网或多租户设计的**，请勿如此部署。

### 3. 控制台与鉴权

- 网页控制台（`/`、`/static/`）**不经过鉴权中间件**（其中不含机密，仅为 HTML/CSS/JS）。
- 控制台前端**会携带** `-api-key`：密钥保存在浏览器 `localStorage`（键名 `agent2api_key`，**明文、同源脚本可读、随本机浏览器数据持久化**），由前端自动注入到每个 `/api/*` 与对话页 `/v1/chat/completions` 请求。首次 401 时弹窗索取密钥，经 `/api/status` 验证通过后才落盘。因此**开启鉴权不会弄坏控制台**——只有本地尚未保存（或保存了错误）密钥时才会 401。`/api/auth-hint` 是唯一不鉴权的管理端点，仅返回「是否启用了鉴权」的布尔值。
- `/api/status` 会暴露平台名与账号昵称（默认平台）；`/api/accounts` 与 `/api/accounts/models` 会暴露全部账号的**标签（含昵称）**、调度/冷却状态与各平台可用模型清单；`/health` 只返回状态与平台 ID 列表，不含账号信息。
- `/health` **无条件返回 200**，不探测上游可用性。

### 4. 其他

- `withAuth` 使用**非常量时间比较**校验 API Key（`==`）。
- 错误分类对错误消息做**子串匹配**，但只匹配语义化词组（`rate limit` / `quota` / `context length` / `token limit` / `unauthorized` / `invalid api key` 等），**刻意不匹配裸数字**（`"429"` / `"401"` 已被排除，`internal/llm/failure.go`）。因此回显了这些词组的上游消息仍可能被误判分类；可靠判定来自 HTTP 状态码的显式置位。
- 上游返回的错误消息按**字节**截断至 500 字节，可能截断多字节字符。
- 上游响应会经 `utf-8 → GBK → GB18030 → 替换字符（U+FFFD）` 兜底解码（`internal/adapter/workbuddy/client.go` 的 `decodeBytes`）：无法识别的字节被替换成 U+FFFD（显现为乱码），而不是逐字节映射。

---

## 不属于本项目范围

以下**不属于**本项目的安全范围，请勿作为漏洞报告：

- 上游平台自身的安全问题
- 「上游服务条款是否允许本项目」这类合规问题（见 [DISCLAIMER.md](DISCLAIMER.md)）
- 需要本机已被攻陷（同用户任意代码执行）才能利用的问题
- 已知缺陷清单中已列出的问题（见 [README.md](README.md#已知限制)）

---

## 使用者的责任

- 只在**你拥有授权的账号**上使用
- 不要将凭证文件拷贝到其他机器或提交进仓库
- 不要在**未设置** `-api-key` 的情况下监听 `0.0.0.0`
- 定期检查上游账号的异常用量
